# Build artifacts
target/
**/target/
node_modules/
**/node_modules/
pkg/
**/pkg/
dist/
**/dist/

# Cargo / Rust
Cargo.lock.bak
*.rlib
*.rmeta
*.pdb

# Editor / OS
.vscode/
.idea/
.vs/
.fleet/
.zed/
*.sublime-workspace
*.sublime-project
.DS_Store
._*
.AppleDouble
.LSOverride
Thumbs.db
desktop.ini
*.swp
*~

# Claude Code workspace config (machine-local, per-user)
.claude/

# Internal design doc, not part of the public surface yet.
# Rewrite + move to docs/DESIGN.md before deleting this line.
PLAN.md

# Backup / temp / merge leftovers
*.bak
*.tmp
*.orig
*.rej
*.old

# Test coverage / profiling
coverage/
*.profraw
*.profdata
lcov.info

# Direnv
.envrc
.direnv/

# Cryptographic material — NEVER commit
*.bin
*.key
*.pem
*.p12
*.pfx
*.jks
*.cer
*.crt
*.csr
*.der
*.gpg
*.asc
signing_key*
private_key*
*.priv

# SSH keys (defense against accidental copy from ~/.ssh)
id_rsa*
id_ed25519*
id_ecdsa*
id_dsa*
known_hosts

# Cloud / registry credentials
.cargo/config.toml
.cargo/credentials.toml
.npmrc
.aws/
.gcloud/
.azure/
credentials.json
credentials.yaml
auth.json
token.json
tokens.json

# Local data / WAL / scratch
dev-data/
poc-data/
demo-data/
scratch/
tmp/

# Phase 2 seeder outputs (NEVER commit signed demo WAL or pubkey to this repo
# until they are deliberately copied into spine-public/playground/assets/
# during Phase 5 hosting).
demo-seeder/target/
demo-seeder/out/
demo-seeder/out-test/
demo.jsonl
demo.pubkey
demo.expected_root
demo-manifest.json

# wasm-pack output — built artifacts, regenerated by Phase 3 pipeline
spine-wasm/pkg/

# Phase 5 build pipeline staging directory
playground-spec/dist/

# Environment
.env
.env.*
!.env.example

# Logs
*.log

# Spike/throwaway folders (must be explicitly opted-in)
spike-*/
